# config/cucumber.yml
##YAML Template
---
default:
  worldParameters:
    browser: firefox
firefox:
  worldParameters:
    browser: firefox
chrome:
  worldParameters:
    browser: chrome
james:
  worldParameters:
    browser: chrome
headless: HEADLESS_OPTION=--headless
tags: --tags ~@test
html_report: -b --format html --out ./reports/TestResults.html --format pretty
paths: 'test/features/**/*.feature'